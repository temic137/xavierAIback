services:
  - type: web
    name: xavierAIback
    env: python
    buildCommand: ./build.sh
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.18
    buildFilter:
      paths:
        - requirements.txt
        - "**/*.py"
        - build.sh
